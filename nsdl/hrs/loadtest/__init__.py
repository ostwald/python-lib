"""
module to hit the HRS server
				 		retrieveHandle	|  createHandle	
- time per hit		|					|
- time per x hits	|					|

Working assumptions:
	Ingest is heaviest load:
		- 1 metadataHandle lookup
		- 1.1 resourceHandle lookups
	(per record ingested)
	
"""

